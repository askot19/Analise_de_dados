-- 3. What is the total revenue generated by each salesperson?

SELECT
    sp.name
  , SUM(c.cost_$) AS total_revenue_by_salesperson
FROM
  salespersons AS sp
INNER JOIN
  sales AS s
  ON
    sp.salesman_id = s.salesman_id
INNER JOIN
  cars AS c
  ON
    c.car_id = s.car_id
GROUP BY
  sp.name
ORDER BY
  sp.name;
